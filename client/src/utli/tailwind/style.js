export const navbar = {
    navLink: ({ isActive }) =>
        isActive
          ? "text-green-500 font-semibold" // Active link style
          : "text-black hover:text-green-500" // Normal & hover style
      
  
  };

  